// helper.js - Các hàm trợ giúp

// Hàm kiểm tra xem một đối tượng có rỗng hay không
function isEmpty(obj) {
    return Object.keys(obj).length === 0;
}

// Hàm hiển thị thông báo cho người dùng
function showAlert(message) {
    alert(message);
}
